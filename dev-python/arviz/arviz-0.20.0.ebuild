# This ebuild was auto-generated by homeassistant script, and the script was not able to find a better candidate.
EAPI=8

DISTUTILS_USE_PEP517=setuptools
PYTHON_COMPAT=( python3_{12,13{,t}} )
GENERATED_IUSE="all preview"
IUSE="${GENERATED_IUSE}"

inherit distutils-r1 pypi

DESCRIPTION=""
HOMEPAGE="
  https://pypi.org/project/arviz/"

LICENSE="Apache-2.0"
SLOT="0"
KEYWORDS="amd64 arm64"

REQUIRES_DIST="
	arviz-base[h5netcdf]; extra == 'preview'
	arviz-plots; extra == 'preview'
	arviz-stats[xarray]; extra == 'preview'
	bokeh>=3; extra == 'all'
	contourpy; extra == 'all'
	dask[distributed]; extra == 'all'
	dm-tree>=0.1.8; extra == 'all'
	h5netcdf>=1.0.2
	matplotlib>=3.5
	netcdf4; extra == 'all'
	numba; extra == 'all'
	numpy>=1.23.0
	packaging
	pandas>=1.5.0
	scipy>=1.9.0
	setuptools>=60.0.0
	typing-extensions>=4.1.0
	ujson; extra == 'all'
	xarray-datatree; extra == 'all'
	xarray-einstats>=0.3
	xarray>=2022.6.0
	zarr<3,>=2.5.0; extra == 'all'
"
GENERATED_RDEPEND="${RDEPEND}
	preview? ( dev-python/arviz-base[h5netcdf,${PYTHON_USEDEP}] )
	preview? ( dev-python/arviz-plots[${PYTHON_USEDEP}] )
	preview? ( dev-python/arviz-stats[xarray,${PYTHON_USEDEP}] )
	all? ( >=dev-python/bokeh-3[${PYTHON_USEDEP}] )
	all? ( dev-python/contourpy[${PYTHON_USEDEP}] )
	all? ( dev-python/dask[distributed,${PYTHON_USEDEP}] )
	all? ( >=dev-python/dm-tree-0.1.8[${PYTHON_USEDEP}] )
	>=dev-python/h5netcdf-1.0.2[${PYTHON_USEDEP}]
	>=dev-python/matplotlib-3.5[${PYTHON_USEDEP}]
	all? ( dev-python/netcdf4[${PYTHON_USEDEP}] )
	all? ( dev-python/numba[${PYTHON_USEDEP}] )
	>=dev-python/numpy-1.23.0[${PYTHON_USEDEP}]
	dev-python/packaging[${PYTHON_USEDEP}]
	>=dev-python/pandas-1.5.0[${PYTHON_USEDEP}]
	>=dev-python/scipy-1.9.0[${PYTHON_USEDEP}]
	>=dev-python/setuptools-60.0.0[${PYTHON_USEDEP}]
	>=dev-python/typing-extensions-4.1.0[${PYTHON_USEDEP}]
	all? ( dev-python/ujson[${PYTHON_USEDEP}] )
	>=dev-python/xarray-2022.6.0[${PYTHON_USEDEP}]
	all? ( dev-python/xarray-datatree[${PYTHON_USEDEP}] )
	>=dev-python/xarray-einstats-0.3[${PYTHON_USEDEP}]
	all? ( >=dev-python/zarr-2.5.0[${PYTHON_USEDEP}] <dev-python/zarr-3[${PYTHON_USEDEP}] )
"
RDEPEND="${GENERATED_RDEPEND}"

distutils_enable_tests pytest
