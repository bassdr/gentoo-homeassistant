# This ebuild was auto-generated by homeassistant script, and the script was not able to find a better candidate.
EAPI=8

DISTUTILS_USE_PEP517=setuptools
PYTHON_COMPAT=( python3_{12,13{,t}} )
GENERATED_IUSE="hf-datasets pandas-datasets signing"
IUSE="${GENERATED_IUSE}"

inherit distutils-r1 pypi

DESCRIPTION=""
HOMEPAGE="
  https://pypi.org/project/kagglehub/"

LICENSE=""
SLOT="0"
KEYWORDS="amd64 arm64"

REQUIRES_DIST="
	betterproto>=2.0.0b6; extra == 'signing'
	datasets; extra == 'hf-datasets'
	model-signing; extra == 'signing'
	packaging
	pandas; extra == 'hf-datasets'
	pandas; extra == 'pandas-datasets'
	pyyaml
	requests
	sigstore>=3.6.1; extra == 'signing'
	tqdm
"
GENERATED_RDEPEND="${RDEPEND}
	signing? ( >=dev-python/betterproto-2.0.0_beta6[${PYTHON_USEDEP}] )
	hf-datasets? ( dev-python/datasets[${PYTHON_USEDEP}] )
	signing? ( dev-python/model-signing[${PYTHON_USEDEP}] )
	dev-python/packaging[${PYTHON_USEDEP}]
	hf-datasets? ( dev-python/pandas[${PYTHON_USEDEP}] )
	pandas-datasets? ( dev-python/pandas[${PYTHON_USEDEP}] )
	dev-python/pyyaml[${PYTHON_USEDEP}]
	dev-python/requests[${PYTHON_USEDEP}]
	signing? ( >=dev-python/sigstore-3.6.1[${PYTHON_USEDEP}] )
	dev-python/tqdm[${PYTHON_USEDEP}]
"
RDEPEND="${GENERATED_RDEPEND}"

distutils_enable_tests pytest
