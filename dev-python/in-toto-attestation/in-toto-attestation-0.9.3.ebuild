# This ebuild was auto-generated by homeassistant script, and the script was not able to find a better candidate.
EAPI=8

DISTUTILS_USE_PEP517=setuptools
PYTHON_COMPAT=( python3_{12,13{,t}} )
GENERATED_IUSE="lint"
IUSE="${GENERATED_IUSE}"

inherit distutils-r1 pypi

DESCRIPTION=""
HOMEPAGE="
  https://pypi.org/project/in-toto-attestation/"

LICENSE=""
SLOT="0"
KEYWORDS="amd64 arm64"

REQUIRES_DIST="
	build; extra == 'dev'
	coverage[toml]; extra == 'test'
	in-toto-attestation[lint,test]; extra == 'dev'
	mypy; extra == 'lint'
	protobuf
	pytest-cov; extra == 'test'
	pytest; extra == 'test'
	ruff; extra == 'lint'
	types-protobuf; extra == 'lint'
"
GENERATED_RDEPEND="${RDEPEND}
	lint? ( dev-python/mypy[${PYTHON_USEDEP}] )
	dev-python/protobuf[${PYTHON_USEDEP}]
	lint? ( dev-python/ruff[${PYTHON_USEDEP}] )
	lint? ( dev-python/types-protobuf[${PYTHON_USEDEP}] )
"
RDEPEND="${GENERATED_RDEPEND}"

distutils_enable_tests pytest
GENERATED_BDEPEND="${BDEPEND}
	test? (
		dev-python/build[${PYTHON_USEDEP}]
		dev-python/coverage[toml,${PYTHON_USEDEP}]
		dev-python/in-toto-attestation[lint,test,${PYTHON_USEDEP}]
		dev-python/pytest[${PYTHON_USEDEP}]
		dev-python/pytest-cov[${PYTHON_USEDEP}]
	)
"
BDEPEND="${GENERATED_BDEPEND}"
