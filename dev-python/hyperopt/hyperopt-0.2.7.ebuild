# This ebuild was auto-generated by homeassistant script, and the script was not able to find a better candidate.
EAPI=8

DISTUTILS_USE_PEP517=setuptools
PYTHON_COMPAT=( python3_{12,13{,t}} )
GENERATED_IUSE="atpe mongotrials sparktrials test"
IUSE="${GENERATED_IUSE}"

inherit distutils-r1 pypi

DESCRIPTION=""
HOMEPAGE="
  https://pypi.org/project/hyperopt/"

LICENSE="BSD"
SLOT="0"
KEYWORDS="amd64 arm64"

REQUIRES_DIST="
	black ; extra == 'dev'
	cloudpickle
	future
	lightgbm ; extra == 'atpe'
	networkx (>=2.2)
	nose ; extra == 'dev'
	numpy
	pre-commit ; extra == 'dev'
	py4j
	pymongo ; extra == 'mongotrials'
	pyspark ; extra == 'sparktrials'
	pytest ; extra == 'dev'
	scikit-learn ; extra == 'atpe'
	scipy
	six
	tqdm
"
GENERATED_RDEPEND="${RDEPEND}
	dev-python/cloudpickle[${PYTHON_USEDEP}]
	dev-python/future[${PYTHON_USEDEP}]
	atpe? ( dev-python/lightgbm[${PYTHON_USEDEP}] )
	>=dev-python/networkx-2.2[${PYTHON_USEDEP}]
	dev-python/numpy[${PYTHON_USEDEP}]
	dev-python/py4j[${PYTHON_USEDEP}]
	mongotrials? ( dev-python/pymongo[${PYTHON_USEDEP}] )
	sparktrials? ( dev-python/pyspark[${PYTHON_USEDEP}] )
	atpe? ( dev-python/scikit-learn[${PYTHON_USEDEP}] )
	dev-python/scipy[${PYTHON_USEDEP}]
	dev-python/six[${PYTHON_USEDEP}]
	dev-python/tqdm[${PYTHON_USEDEP}]
"
RDEPEND="${GENERATED_RDEPEND}"

distutils_enable_tests pytest
GENERATED_BDEPEND="${BDEPEND}
	test? (
		dev-python/black[${PYTHON_USEDEP}]
		dev-python/nose[${PYTHON_USEDEP}]
		dev-python/pytest[${PYTHON_USEDEP}]
		dev-vcs/pre-commit[${PYTHON_USEDEP}]
	)
"
BDEPEND="${GENERATED_BDEPEND}"
